# bootstrap-content-version: bytewyrd/mise.toml@ebf354bf:e3b0c44298fc
# mise.toml — tool version pins (managed by /sync)
# Add language-specific tools as needed; non-Rust languages use mise.
# Rust is managed via rust-toolchain.toml + rustup (not mise).

[tools]
