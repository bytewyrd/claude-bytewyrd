# mise.toml — tool version pins (managed by /sync)
# The [tools] table is yours to maintain: add non-Rust language pins as needed,
# e.g. bun = "1.1.0", go = "1.22.0", python = "3.12.0" (resolve current versions
# with `mise latest <tool>`; never write "latest").
# Rust is managed via rust-toolchain.toml + rustup (not mise).

[tools]
